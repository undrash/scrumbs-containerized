{
  "name": "scrumbs-containerized",
  "scripts": {
    "start:development": "docker-compose -f docker-compose.dev.yml down && npm run build-dev --prefix client/ && npm run build-dev --prefix website/ && docker-compose -f docker-compose.dev.yml build && docker-compose -f docker-compose.dev.yml up",
    "start:production": "docker-compose down && npm run build --prefix client/ && npm run build --prefix website/ && docker-compose build && docker-compose up -d"
  }
}
