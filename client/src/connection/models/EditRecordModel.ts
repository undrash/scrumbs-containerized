


export class EditRecordModel {
    public recordId: string;
    public name: string;

    constructor(recordId: string, name: string) {
        this.recordId   = recordId;
        this.name       = name;
    }
}